"use client";

import { CustomButton } from "@/components/CustomComponents";

export const ProjectSelect = () => {
  return <CustomButton className="!bg-white !text-black-500">Select app</CustomButton>;
};
